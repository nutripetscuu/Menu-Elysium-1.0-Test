# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Development Settings
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:9002

# Site URL Configuration (CRITICAL for QR Codes)
# For local development: http://localhost:9002
# For production: Your actual domain (e.g., https://nowaiter.app or https://your-project.vercel.app)
# NOTE: On Vercel, if not set, it will automatically use VERCEL_URL
NEXT_PUBLIC_SITE_URL=http://localhost:9002

# Multi-Tenant Configuration
# For localhost development: Set this to a valid restaurant ID to use as default
# In production, this is NOT used - domain/subdomain detection is automatic
DEFAULT_RESTAURANT_ID=your_default_restaurant_id_for_localhost

# Google AI Configuration (if using AI features)
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Security Keys (generate your own for production)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:9002

# Telegram Bot Configuration
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=your_telegram_bot_token
NEXT_PUBLIC_TELEGRAM_CHAT_ID=your_telegram_chat_id

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env.local to version control
# 4. For production deployment, set these as environment variables in your hosting platform
